apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ghost-pvc
  namespace: ghost-blog
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp3-csi  # default AWS storage class for EBS dynamic provisioning
  resources:
    requests:
      storage: 10Gi
